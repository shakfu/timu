# timu

A team of specialised LLM agents. Each agent has a role that fixes its prompt,
tools and permissions. Workflows combine agents toward one objective.

`timu` is Swahili for "team".

Status: early development. See `docs/dev/design.md` for the design and
`docs/dev/plan.md` for the implementation plan.
